#include <iostream>
using namespace std;
/*

Ej. MI-27: En un torneo de fútbol participan K (cantidadEquipos) equipos. El torneo se juega con el sistema de todos contra todos. 
Por cada partido disputado por un equipo se dispone de la siguiente información:
a) Nro. de equipo,
b) Código del resultado ('P'= Perdido, 'E'= Empatado, 'G'= Ganado).
Se arma un lote de datos con todos los resultados del torneo, agrupados por Nro. de equipo.
Desarrollar el programa que imprima:
Por cada equipo, su número y el puntaje total que obtuvo (suma 3 si gana, y 1 si empata).

*/
int main () 
{

    int cantidadEquipos = 0;

    cout << "Ingrese la cantidad de equipos: " << endl;
    cin >> cantidadEquipos;

    //evaluamos primer equipo 
    for (int i = 0; i < cantidadEquipos; i++)
    {
        int numeroEquipo = 0;
        
        // pedir resultados de ese equipo contra todos los demas
        // acumulas el puntaje
        for (int i = 0; i < cant; i++)
        {
            /* code */
        }

        //mostras rrsultapos de ese equipo
        
    }
    

while ()
{
    /* code */
}

    
 
system("pause");
 return 0;
}